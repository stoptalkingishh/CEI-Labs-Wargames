# Sentinel Lab Design Matrix

## Purpose

This matrix turns the Sentinel curriculum plan into reviewable lab contracts.
It describes the proposed mechanism and validation for each lab; a `Planned`
row is not an implementation claim. All labs use the per-team Sentinel SSH
target unless the row explicitly says static artifact. No lab uses public
targets, live malware, production evidence, unrestricted network access, or
generic privilege escalation.

`Built` refers to [PR #68](https://github.com/stoptalkingishh/CEI-Labs-Wargames/pull/68),
which remains non-staged pending platform integration.

| Lab | Status | Objective and learner outcome | How the lab accomplishes it | Validation | Software and artifacts | Proposed build |
| --- | --- | --- | --- | --- | --- |
| Start Here | Built | Secure SSH access; safely use the environment. | Learner connects with supplied bootstrap credentials, reads rules, and retrieves an onboarding token. | Dynamic onboarding flag; SSH connection test; no public listener except SSH. | OpenSSH; generated banner/rules/token. | `build_sentinel.py` challenge plus target account and deterministic token material. |
| 01 Asset Census | Built | 4.1, 4.2: reconcile an inventory with real host state. | Compare a supplied CMDB-style record with installed packages, active services, ports, and ownership data. | Dynamic flag is in the correctly identified asset record; expected command/output fixture test. | Core Linux tools: `dpkg`, `systemctl`, `ss`, `stat`; synthetic inventory. | Build-time fixtures; runtime secret renders the next credential only in the intended evidence. |
| 02 Control Review | Built | 1.1, 1.2: classify controls and relate them to CIA/AAA. | Inspect control cards and host evidence to identify the required category and control type. | Flag follows the only evidence-supported classification; content test rejects ambiguous alternatives. | Static control cards and local host configuration excerpts. | Versioned text artifacts under the target; generator description and hints use original scenarios. |
| 03 Change Window | Built | 1.3: recognize security-relevant change-management gaps. | Review a change ticket, approval trail, test result, dependency note, and rollback plan. | Flag is associated with the missing required control; artifact contract checks required/absent fields. | Static change-management records and a local Git-style diff. | Build-time records, fixed timestamps, and no service mutation needed. |
| 04 Certificate Trail | Built | 1.4: assess certificate trust and key handling. | Inspect a supplied certificate chain, private-key permissions, and local revocation evidence. | Dynamic flag follows the trustworthy service identity; OpenSSL/permission test confirms the intended result. | OpenSSL; generated test CA, certificates, CRL/OCSP-like fixture. | Build test PKI fixtures; entrypoint derives only the next credential, never a production-style secret. |
| 05 Attack Surface | Built | 2.2, 2.5: identify exposed service risk and hardening action. | Enumerate listeners and service configuration, then identify an unintended default service. | Dynamic flag is in the intended configuration evidence; wrong-user access and SSH-only exposure tests pass. | `ss`, `systemctl`, OpenSSH; static service configuration. | Keep only SSH live; represent additional services as evidence/configuration, not exposed daemons. |
| 06 Suspicious Access | Planned, batch 1 | 2.4, 4.4, 4.9: identify a password-spray chain and scope it. | Correlate deterministic `auth.log`, `last`, and optional narrowly scoped audit events with benign noise. | Learner submits/retrieves the account, source, and time-window credential from raw logs; timeline fixture test and wrong-user denial pass. | `journalctl`, `last`, `grep`, `awk`, optional `ausearch`; synthetic auth/audit logs. | Generate fixed log corpus with a documented ground-truth timeline; derive only final credential at startup. |
| 07 Vulnerability Triage | Planned, batch 1 | 2.3, 4.3: confirm a finding, assess context, and choose remediation priority. | Compare a mock scanner finding/CVE advisory with package version, compensating control, and asset criticality. | Flag requires identifying true/false status and priority; tests assert a single evidence-supported answer. | Static advisory, scan output, package inventory, risk rubric. | No live scanner/feed. Store versioned text fixtures and a deterministic scoring worksheet. |
| 08 Least Privilege | Planned, batch 1 | 2.5, 4.6: audit and correct over-broad delegated access. | Inspect users, groups, ACLs, and `sudoers` fragments; find the permission that violates role scope. | Flag is readable only after the intended least-privilege finding; tests prove neighboring accounts cannot read it. | `id`, `getfacl`, `sudo -l`, `stat`; controlled groups/ACL fixtures. | Build account/group model and non-world-traversable homes; no root shell or unbounded `sudo`. |
| 09 Segmented Services | Planned, batch 2 | 3.1, 3.2: choose a safe management path through zones. | Interpret a topology and host-firewall policy to find an unsafe permitted flow. | Flag depends on the correct zone/rule conclusion; policy parser/fixture test rejects alternate flows. | Static diagram and `nftables`-style ruleset; optional local `nft` syntax validation. | Static first. If a live policy is needed, use a private network namespace and never grant host `CAP_NET_ADMIN`. |
| 10 Data Handling | Planned, batch 2 | 3.3: classify data and select protection by state. | Review records with data type, classification, geography, and use case; choose encryption/masking/tokenization/access restriction. | Flag requires the only policy-consistent handling plan; content test covers each record state. | Static data-classification matrix and policy excerpts; OpenSSL only if a bounded verification adds value. | Synthetic records, no real PII; fixed answer schema in a local worksheet or flag artifact. |
| 11 Recovery Evidence | Planned, batch 2 | 3.4, 5.2: validate backup/recovery readiness and RTO/RPO. | Inspect encrypted-backup metadata, retention, recovery drill, and BIA values. | Flag follows verified RPO/RTO and successful restore evidence; fixture test checks internal consistency. | Static backup manifests and drill logs; optional restic local repository. | Start with static artifacts; add restic only for a deterministic local restore test using synthetic data. |
| 12 Baseline Drift | Planned, batch 3 | 4.1, 4.7: detect and safely remediate baseline drift. | Compare approved baseline to current service/configuration state and use a restricted idempotent helper to restore it. | Post-change verification matches baseline; helper test proves idempotence and denies arbitrary commands. | Core Linux tools; optional Lynis/osquery report; restricted shell helper. | Build approved/current fixtures plus a fixed-command helper owned by an admin group, not broad `sudo`. |
| 13 Integrity Alert | Planned, batch 3 | 1.4, 4.4, 4.5: investigate a file-integrity alert. | Compare hash manifest, package ownership, and change record to identify an unauthorized modification. | Flag follows the affected asset and valid containment decision; hash/owner fixture test is deterministic. | `sha256sum`, `dpkg -S`; optional AIDE-style report. | Synthetic protected files and signed/hashed manifests; no real malicious file. |
| 14 Identity Lifecycle | Planned, batch 3 | 4.6: verify secure offboarding. | Review an offboarding ticket against local account, group, SSH-key, and credential state. | Flag requires identifying incomplete deprovisioning; tests verify target account has no unintended active access. | `getent`, `id`, `find`, authorized-key and group fixtures. | Safe simulated account records; if a mutation is used, expose only a restricted deprovision helper. |
| 15 Alert Tuning | Planned, batch 3 | 4.4: distinguish true alert from false positive and document tuning. | Query normalized, SIEM-like JSON events with timestamps and benign noise. | Flag derives from the correctly scoped event; fixture test checks query result and rationale fields. | `jq`, `grep`, JSON event corpus; Suricata EVE JSON may be added after offline PCAP work. | Versioned JSON logs with checksum and ground-truth labels withheld from learner. |
| 16 Secure Service Change | Planned, batch 3 | 4.5, 4.7: apply a reversible security change through automation. | Review a change request and run a restricted, idempotent helper that updates only a safe training configuration. | Automated test verifies precondition, exact allowed mutation, backout, idempotence, and no shell escape. | Shell/Python helper, static firewall/service configuration; optional `nft` namespace. | No generic configuration write access. Helper accepts no arbitrary paths/commands and logs its action. |
| 17 Incident Containment | Planned, batch 4 | 2.4, 4.8, 4.9: build a timeline, scope incident, choose preservation-safe containment. | Correlate auth, process, application, firewall, and PCAP evidence from one controlled incident. | Flag represents correct containment; test asserts timeline ordering, scope, and that evidence corpus remains unchanged. | `grep`, `jq`, `tcpdump -r`; optional offline Suricata EVE JSON. | Create one timestamped synthetic incident corpus with benign distractors and a documented instructor timeline. |
| 18 Forensic Handoff | Planned, batch 4 | 4.8: preserve and document evidence. | Verify hashes, evidence manifest, and chain-of-custody handoff without altering evidence. | Flag follows successful manifest validation; test checks source evidence hashes are unchanged after intended workflow. | `sha256sum`, `stat`, signed/hash manifest, chain-of-custody form. | Evidence directory mounted/readable as appropriate; no writable raw evidence and no real forensic data. |
| 19 Policy to Practice | Planned, batch 5 | 5.1: distinguish governance documents and accountable roles. | Match host evidence to policy, standard, procedure, owner/controller/processor/custodian responsibilities. | Flag requires correctly locating the governance gap and accountable role; content test prevents multiple valid matches. | Static policy/standard/procedure/RACI and host evidence package. | Original fictional documents with stable version/revision metadata. |
| 20 Risk Register | Planned, batch 5 | 5.2: calculate and prioritize risk, then select treatment. | Use a small risk register/BIA to calculate SLE, ARO, ALE and compare RTO/RPO with risk appetite. | Flag derives from correct calculation and treatment; unit tests validate formulas, rounding, and unique priority. | Static CSV/JSON risk register and calculator worksheet; shell/Python arithmetic only. | Fixed fictional values with transparent formulas; no external GRC tool. |
| 21 Assurance Review | Planned, batch 5 | 2.1, 5.3-5.6: evaluate vendor, compliance, audit, and phishing evidence. | Review a vendor pack, agreement excerpt, assessment record, and suspicious-message report. | Flag requires required escalation and record; content test validates evidence-to-decision mapping. | Static vendor questionnaire, SLA/MOU/NDA excerpts, audit/attestation, email/header artifact. | Original documents and synthetic email data; no live email, reputation lookup, or external service. |

## Cross-lab validation requirements

- Every scored challenge has a unique per-team dynamic flag, three progressive
  managed hints, an instructor writeup, and a YAML/content invariant test.
- Dynamic-secret labs prove fail-closed startup, deterministic restart output,
  secret scrubbing before SSH starts, and cross-account denial.
- Artifact-only labs use stable timestamps, fictional data, checksums for
  binary evidence, and a test that confirms one intended answer.
- Any mutation helper is command- and path-restricted, idempotent, reversible,
  logged, and tested for shell escape and unauthorized state changes.
- Offline evidence tools use pinned versions, local artifacts, and no network
  feeds, live capture, or external DNS.
