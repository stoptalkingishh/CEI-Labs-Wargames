<?php require '/etc/cei-labs/natas-runtime/natas32.php'; $command = isset($_POST['command']) && is_string($_POST['command']) ? $_POST['command'] : ''; $output = array('status' => 'ready', 'debrief' => 'handoff'); $ok = strlen($command) <= 16 && isset($output[$command]) && $command === 'debrief'; ?><!doctype html><html><body><h1>Fixed command emulator</h1><p>The emulator maps one allowlisted command label to fixed text. Nothing executes.</p><form method="post"><input name="command" maxlength="16"><button>Emulate</button></form><?php if ($ok): ?><p>Emulator handoff: <?php echo htmlspecialchars($natas33_secret, ENT_QUOTES, 'UTF-8'); ?></p><?php else: ?><p>Fixed output only.</p><?php endif; ?></body></html>
